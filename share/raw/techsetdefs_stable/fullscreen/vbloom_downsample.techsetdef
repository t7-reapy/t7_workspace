#include "state"
#include "renderFlags"

Globals()
{
	category = "Filters"
}

Technique( "lit", "unlit" )
{
	state = "disable"

	vs = VertexShader()
	{
		source = "vbloom_downsample.hlsl"
	}
	ps = PixelShader()
	{
		source = "vbloom_downsample.hlsl"

		biLinearClamp = Sampler("no tile", "nomip bilinear")
	}
}
