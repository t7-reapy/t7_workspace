#include "uie_base"

Globals()
{
	category = "2d"
	renderFlags = "none"
}


Sampler( "colorSampler" )
{
	tile = <tileColor>
	filter = <filterColor>
	tweak = Tweak()
	{
		category  = "Color"
		sortindex = "10"
	}
}

Technique( "unlit" )
{
	state = "multiply + nocull"
	vs = VertexShader()
	{
#if TOOLSGFX == "1"
		source = "ToolsGfx/simple.hlsl"
		vertexDecl = "generic"
#else
		source = "uie/ui_multiply.hlsl"
#endif
	}
	ps = PixelShader()
	{
#if TOOLSGFX == "1"
		source = "ToolsGfx/simple.hlsl"
#else
		source = "uie/ui_multiply.hlsl"
		colorMapSampler = CodeTexture( "codeTexture0" )
#endif
	}
}
