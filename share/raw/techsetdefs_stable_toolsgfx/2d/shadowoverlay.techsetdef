#include "state"
#include "renderFlags"

Globals()
{
	category = "2d"
	renderFlags = "none"
}

Sampler( "shadowoverlaySampler" )
{
	tile = "tile both"
	filter = "mip standard (2x bilinear)"
}

Technique( "unlit" )
{
	state = "replace + nocull"
	vs = VertexShader()
	{
		source = "techsetdef_shadowoverlay.hlsl"
	}
	ps = PixelShader()
	{
		source = "techsetdef_shadowoverlay.hlsl"
		float2DArraySampler = CodeTexture( "feedback" )
	}
}
