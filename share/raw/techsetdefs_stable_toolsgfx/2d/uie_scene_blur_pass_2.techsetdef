#include "uie_base"

Globals()
{
	category = "2d"
	renderFlags = "none"
}

Sampler( "tileMipBilinearSampler" )
{
	tile = "tile both"
	filter = "mip standard (2x bilinear)"
}

Sampler( "tileNoMipNearest" )
{
	tile = "no tile"
	filter = "nomip nearest"
}

Technique( "unlit" )
{
	state = "blend + nocull"
	vs = VertexShader()
	{
#if TOOLSGFX == "1"
		source = "ToolsGfx/simple.hlsl"
		vertexDecl = "generic"
#else
		source = "uie/uie_scene_blur_pass_2.hlsl"
#endif
	}
	ps = PixelShader()
	{
#if TOOLSGFX == "1"
		source = "ToolsGfx/simple.hlsl"
#else
		source = "uie/uie_scene_blur_pass_2.hlsl"
		colorMapSampler = CodeTexture( "codeTexture0" )
		tileMipBilinearSampler = "tileMipBilinearSampler"
		tileNoMipNearest = "tileNoMipNearest"
#endif
	}
}
