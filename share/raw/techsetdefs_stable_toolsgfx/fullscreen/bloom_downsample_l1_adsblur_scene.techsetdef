#include "state"
#include "renderFlags"

Globals()
{
	category = "Filters"
}

Technique( "lit", "unlit" )
{
	state = "disable"
	source = "BLOOM/bloom_downsample_l1.hlsl"
	defines = "ADSBLUR_DOWNSAMPLE 1", "DOWNSAMPLE_SCENE 1"

	vs = VertexShader()
	{

	}
	ps = PixelShader()
	{
		biLinearClamp = Sampler("no tile", "nomip bilinear")
	}
}
