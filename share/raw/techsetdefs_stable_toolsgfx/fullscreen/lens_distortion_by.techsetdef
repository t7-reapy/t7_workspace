#include "state"
#include "renderFlags"

Globals()
{
	category = "Filters"
}


Technique( "lit", "unlit" )
{
	state = "disable"

	vs = VertexShader()
	{
		source = "lens_distortion_by.hlsl"
	}
	ps = PixelShader()
	{
		source = "lens_distortion_by.hlsl"

		// textures
		codeTexture0 = CodeTexture( "codeTexture0" )

		// samplers
		bilinearClamp = Sampler("no tile", "nomip bilinear")
	}
}
