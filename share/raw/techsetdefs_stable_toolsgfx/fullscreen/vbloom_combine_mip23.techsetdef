#include "state"
#include "renderFlags"

Globals()
{
	category = "Filters"
}

Technique( "lit", "unlit" )
{
	state = "disable"

	vs = VertexShader()
	{
		source = "vbloom_combine_mip23.hlsl"
	}
	ps = PixelShader()
	{
		source = "vbloom_combine_mip23.hlsl"

		biLinearClamp = Sampler("no tile", "nomip bilinear")
	}
}
