
#include "state"
#include "renderFlags"

Globals()
{
	category = "Filters"
}

Technique( "lit", "unlit" )
{
	state = "disable"

	vs = VertexShader()
	{
		source = "bloom_blur_y.hlsl"
	}
	ps = PixelShader()
	{
		source = "bloom_blur_y.hlsl"

		bilinearClamp = Sampler("no tile", "nomip bilinear")
	}
}
