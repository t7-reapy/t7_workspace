#include "state"
#include "renderFlags"

Globals()
{
	category = "Filters"
}

Technique( "lit", "unlit" )
{
	state = "disable"

	vs = VertexShader()
	{
		source = "bloom_downsample_convolution.hlsl"
	}
	ps = PixelShader()
	{
		source = "bloom_downsample_convolution.hlsl"

		bilinearClamp = Sampler("no tile", "nomip bilinear")
	}
}
