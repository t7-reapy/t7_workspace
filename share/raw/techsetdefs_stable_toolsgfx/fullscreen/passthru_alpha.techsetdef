
#include "state"
#include "renderFlags"

Globals()
{
	category = "Filters"
}

Technique( "lit", "unlit" )
{
	state = "disable"

	vs = VertexShader()
	{
		source = "pimp_textured_simple.hlsl"
		defines = "USE_VERTCOL 0"
	}
	ps = PixelShader()
	{
		source = "pimp_textured_simple.hlsl"
		defines = "USE_VERTCOL 0"

		colorMapSampler  = CodeTexture( "feedback" )
	}
}
