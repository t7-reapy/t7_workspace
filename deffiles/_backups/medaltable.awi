#include "asset_list_helper.h"
const int MAX_MEDAL_ENTRIES = 50; // changing this does not require updating anything else

void GenerateUI( asset Asset )
{
	GenerateItemList( Asset, "medal", "Medal", "medal", MAX_MEDAL_ENTRIES );
}
